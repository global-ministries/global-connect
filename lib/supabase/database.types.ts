// Este archivo es generado automáticamente por `pnpm gen:types`.
// El stub mínimo permite que tsc compile en CI sin acceso a la DB.
// Para regenerar con datos reales: pnpm gen:types

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Database {}
